services:
  ocr:
    build: .
    image: fast-api-ocr
    ports:
      - "8000:8000"
    environment:
      LOG_PRETTY: "0"
      OCR_MAX_DIMENSION: "2048"
      MAX_UPLOAD_BYTES: "10485760"
    restart: unless-stopped
